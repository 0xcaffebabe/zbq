<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="wang.ismy.zbq.dao.VideoSearchLogMapper">


    <insert id="insertNew">
        INSERT INTO video_search_log(user, kw, create_time,update_time)
                VALUES (#{user.userId},#{kw},#{createTime},NOW())
    </insert>
</mapper>