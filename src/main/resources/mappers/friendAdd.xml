<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="wang.ismy.zbq.dao.FriendAddMapper">
    <insert id="insertNew">
        INSERT INTO friend_add(from_user, to_user, msg, create_time, update_time)
                VALUE(#{fromUser},#{toUser},#{msg},NOW(),NOW()))
    </insert>
    <select id="selectFriendAddByFromUserAndToUser" resultType="wang.ismy.zbq.entity.FriendAdd">
        SELECT * FROM friend_add WHERE from_user = #{from} AND to_user = #{to}
    </select>
</mapper>